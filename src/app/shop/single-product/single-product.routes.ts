import { Routes, ROUTES } from '@angular/router';
import { SingleProductComponent } from './single-product.component';

export const SINGLE_PRODUCT_ROUTES: Routes = [
  { path: '', component: SingleProductComponent },
];
